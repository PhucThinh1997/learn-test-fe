import React, { memo } from 'react'
import PropTypes from 'prop-types'

const Seminar = memo(function Seminar(props) {
  return (
    <div>
      seminar
    </div>
  )
})

Seminar.propTypes = {

}

export default Seminar
